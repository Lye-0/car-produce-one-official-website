numpy>=2.2
scipy>=1.14
trimesh>=4.7
shapely>=2.1
Pillow>=11.0
vtk>=9.4
# Optional UI logic checks (mocked GPU): playwright
# Optional native shader checks: PyOpenGL
# FFmpeg is a separate executable for review movie creation.
