# 2026-09-10 品質設定の見直し

旧1440p/最大256 samplesの短尺テストは、ユーザーの時間短縮の指示により停止。
採用設定（高速化3案と最小8・最大48サンプルをユーザーが採用）: 1920×1080（縦1080×1920）、30fps、最大48 samples、最小8 samples、noise threshold 0.05、最大反射回数6、反射・屈折コースティクス無効、16bit RGB PNG、OpenImageDenoise GPU有効。
旧設定のPNGは quality-final-production-01 に保持。現在の検証画像は sample-final-balanced-01 に分離。CPO_MASTER.blend は変更していない。

同一カメラ・同一フレームの1枚ずつの実測（描画と保存、Blender起動時間を除く）:
- desktop drive frame 240: 84.00秒 → 15.79秒（約5.3倍）
- desktop route frame 1026: 32.32秒 → 10.59秒（約3.1倍）

CPU負荷: MASTERの両シーンで denoising_use_gpu=false を実測確認。旧バッチもこの値を引き継いでいた。描画GPUとは別にCPUデノイズが動いていた。新バッチでは明示的にtrue。シーン準備、画像圧縮・保存などのCPU作業は残る。

夜景の同一領域を1080p相当に揃えて比較し、大きな破綻は見られなかった。連続フレームのノイズ変動と縦映像は新設定では未検証。本番全編は未開始。

本番準備は進行中: 二形式エンコーダーとフロントエンドfallbackの実装・単体検証まで完了。新設定での短尺両形式比較、実ブラウザ検証、配信素材の正式切替は未実施。現在のサイト素材は従来版。


3案はそれぞれ単独で夜景・店内の比較済み。組み合わせた条件の画質・時間は未測定。新しい出力名 final-optimized-01 に分離し、旧設定のフレームと混在させない。


最小8・最大48の組み合わせを採用。本番の出力名は final-optimized-02。デノイズ品質High・16bit RGB PNGを維持。新しい組み合わせの連続動画は未検証。

